{
  "agent_name": "Discord Orchestrator Agent",
  "domain": "core",
  "version": "0.1.0",
  "inputs": [
    {
      "name": "artifact",
      "type": "object",
      "description": "Primary input object for the Discord Orchestrator Agent.",
      "required": true
    },
    {
      "name": "context",
      "type": "object",
      "description": "Optional contextual data (identity, environment, linked resources).",
      "required": false
    }
  ],
  "outputs": [
    {
      "name": "artifact_delta",
      "type": "object",
      "description": "Structured output describing what the Discord Orchestrator Agent decided or performed."
    },
    {
      "name": "logs",
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Human-readable log lines explaining key decisions."
    }
  ],
  "error_handling": {
    "on_invalid_input": "Return a validation error with an explanation; do not attempt to proceed.",
    "on_missing_context": "Proceed conservatively, explicitly stating assumptions and limitations."
  }
}