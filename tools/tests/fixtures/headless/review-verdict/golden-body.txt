<!-- REVIEW-VERDICT v1 agent=diff-reviewer head=a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0 -->
**REVIEW-VERDICT v1 — CHANGES_REQUESTED** · diff-reviewer · head `a1b2c3d`
This is a deliberately long one-sentence summary used to exercise the renderer's 200-character truncation rule so the DryRun validation run has something real to cut, since the design requires a trail… (truncated)

Findings (5 of 6 shown, worst first):
- **blocker** `tools/headless/Invoke-ReadOnlyAgent.ps1:672` — Turn the fallback warning into Exit-UnsafeArg when -JsonSchemaPath is set so a silently-unenforced schema never looks li… (truncated)
- **major** `tools/headless/budgets.json:12` — Remove the hardcoded token [REDACTED] from this file before merging.
- **major** `tools/headless/Invoke-Subagent.ps1:284` — Read envelope.structured_output instead of parsing envelope.result as JSON.
- **minor** `tools/tests/test-invoke-subagent.ps1` — Add a fake-CLI case where the envelope has prose-only result and no structured_output, asserting exit 8.
- **minor** `tools/tests/test-x.ps1:41` — A second minor finding used only to push the total finding count to six so the cap-of-five and the "1 more in the report… (truncated)
1 more in the report: `<source-path>`

Checks: crash-pattern PASS · secret FAIL · ci-no-op FAIL · contract-break FAIL
  searched: gh pr diff 209 for null derefs and unguarded array access; none found.
  searched: grepped the diff for ghp_/gho_/AKIA/-----BEGIN; found one literal token, see findings.
  searched: checked test-invoke-subagent.ps1: no case covers the structured_output-absent path.
  searched: confirmed --json-schema is silently unenforced under an --agent launch on CLI 2.1.280.
Could not check: live acceptance run against a real diff-reviewer launch — no allowedTools roster entry exists yet for diff-reviewer Bash(gh pr diff:*).
Recommendation: Land the #212 fix before merging this PR, or drop the --json-schema claim from the PR body until it does.

_Review comment on head `a1b2c3d` only; a new push needs a new review. Not a MERGE-VERDICT and not a GitHub review; merge authority is unchanged (merge_authority.md)._