# Notion Inventory Pass — 2026-09-16 16:48:06 UTC

Read-only inventory of the Notion account connected to this session (workspace: **the owner's Notion**, `<owner-email>`), ahead of a later effort to migrate Notion content into GitHub Issues per source repo. Nothing in Notion was modified. No GitHub issues were created — this is inventory only.

**Access note:** this connection is on Notion's free/personal MCP tier — `ai_search`, `query_data_sources`, and `query_multiple_data_sources` all report `plan_required`/`full_version_required`, so database row contents couldn't be queried directly (row *counts* below are inferred from page text, not a live query). Enumeration used `list-private-pages`, `list-recent-pages`, and `fetch` (2+ levels deep) instead. `list-shared-pages` and `list-favorite-pages` both returned empty — everything in this workspace is private to Jeremy's account, nothing shared in.

There are **14 top-level items** in the private sidebar. One (`Getting Started on Mobile`) is Notion's own default onboarding page with no user content — excluded from the domain analysis below but listed for completeness.

---

## Top-level inventory

### 1. Applewood Plaza — Portfolio 🏘️
**Content:** Commercial property portfolio hub for two buildings Jeremy owns/manages at Applewood Plaza, North Lexington-Springmill Rd, Mansfield, OH — 331 (14 leasable spaces, main-meter water-reconciliation anomaly flagged for audit) and 311 (6 suites, reconciles clean). Two child property-hub pages, plus 4 shared databases: **Tenants & Units** (~20 rows, all units across both buildings), **Meter Reconciliation** (monthly history Oct 2025–May 2026, ~8 months), **Leases & Contacts** (schema only, not yet filled in), **Maintenance Log** (empty, ready to use). Also references **Meter Readings** and a **Compliance Inspections** database.
**Depth seen:** 3 levels (Portfolio → 331/311 hubs → embedded filtered database views).
**Repo mapping guess:** `business-finance` (Jeremy's own commercial real-estate holding) — **guessing**; could plausibly warrant its own repo given its size (multiple databases, ongoing operational data), but `business-finance` is the closest existing name.

### 2. Nasdara — DayZ PVE Server 🧟
**Content:** Full planning workspace for Jeremy's personal DayZ game server ("Nasdara"), covering game economy design, hosting/deployment ops, faction/quest game design, custom mod development plans, mod research, and a separate community/website/monetization track (Discord server "The AEGIS Directive", domain `aegisdirective.net`, Ko‑fi monetization plan). 7 child pages + 1 Tasks database, each fetched:
  - Roadmap & Priorities, Economy System, Deployment & Hosting, Factions & Quests, Custom Mods — NasdaraCore, Mod Research, Community/Website/Monetization.
**Depth seen:** 2 levels (all children fetched; no further nesting found under them).
**Repo mapping guess:** `site-chernarus` for the core game-server/economy/hosting/faction content (name matches the DayZ Chernarus map) — **guessing**. The custom-mod-development track and the "AEGIS Directive" branded community/site/monetization track could instead belong under `aegis-mods` — **also guessing, genuinely ambiguous**; a human should decide whether this is one repo or split by sub-topic.

### 3. Paul Nielsen — Properties 🔧
**Content:** Client property list for a HandyMansfield maintenance client (LP Nielsen LLC) — 6 properties in Mansfield, OH, 3 of them Section 8/voucher-assisted (subject to mandatory HUD HQS/NSPIRE inspections). Contains owner business mailing address. Embeds **Service Log** and **Compliance Inspections** databases (row counts not directly queryable; many individual job/inspection sub-pages appeared in the recent-pages list, e.g. "Rear siding repair", "Furnace diagnostic & repair", "Masonry repair" — looks like several dozen rows across both clients' logs).
**Depth seen:** 2 levels.
**Repo mapping guess:** `handymansfield` — high confidence, name match is explicit in content.

### 4. Chris Nielsen — Properties 🔧
**Content:** Same pattern as #3, different client — 3 properties, none voucher-assisted (compliance checklist applied as landlord best practice only). Embeds its own **Service Log** and **Compliance Inspections** databases.
**Depth seen:** 2 levels.
**Repo mapping guess:** `handymansfield` — high confidence.

### 5. HUD Housing Quality Standards (HQS/NSPIRE) — Compliance Checklist 🏰
**Content:** Reference checklist (not client-specific) for Section 8 voucher housing compliance items, used by both Nielsen client pages.
**Depth seen:** 1 level (reference page, no children).
**Repo mapping guess:** `handymansfield` — high confidence.

### 6. HandyMansfield Client Template — Residential (Section 8 / HQS-NSPIRE) 🏡
**Content:** Reusable onboarding template page (blank Service Log + Compliance Inspections databases) for new residential maintenance clients.
**Depth seen:** 2 levels (embeds 2 empty template databases).
**Repo mapping guess:** `handymansfield` — high confidence.

### 7. HandyMansfield Client Template — Commercial 🏢
**Content:** Same as #6 but for commercial clients.
**Depth seen:** 2 levels.
**Repo mapping guess:** `handymansfield` — high confidence.

### 8. Commercial Life-Safety & Code Compliance Checklist 🏢
**Content:** Reference checklist (fire/life-safety code, ADA, etc.) used by Applewood Plaza and the commercial client template.
**Depth seen:** 1 level.
**Repo mapping guess:** `handymansfield` (it's the compliance-standard reference for the HandyMansfield service line, even though Applewood also uses it) — **guessing on which repo "owns" a shared reference doc**.

### 9. HandyMansfield — Hub 🛠️
**Content:** Central index for the HandyMansfield maintenance business: current clients, the compliance-inspection service offering (pricing not finalized), onboarding process, and a **Handyman Task Pricing Reference — Mansfield OH** database (~30 rows/tasks — explicitly stated in the page text).
**Depth seen:** 2 levels.
**Repo mapping guess:** `handymansfield` — high confidence.

### 10. Compliance-Inspection Service — Pricing Research 💵
**Content:** Market-research-backed pricing recommendation for the compliance-inspection service tiers (residential $85–110/unit/month, commercial itemized). Cites external market-data sources (Buildium, iPropertyManagement, HUD, etc.), not proprietary data.
**Depth seen:** 1 level.
**Repo mapping guess:** `handymansfield` — high confidence.

### 11–13. Task List / Tasks / Projects (databases, top-level)
**Content:** Generic Notion default task/project-tracking databases (the standard Notion starter templates — "Task List" board, a more featured "Tasks" database with Project/Priority/Sub-tasks, and a "Projects" database). No content visible linking them to a specific business domain; appear to be Jeremy's general-purpose personal task tracker, largely unpopulated/template-state based on schema inspection (row-level query tools weren't available on this plan to confirm actual row counts).
**Depth seen:** 1 level (schema only; couldn't query rows — see access note above).
**Repo mapping guess:** Unclear — could be `personal-growth` or `ops-household`, or simply not migration-relevant since they're generic and not tied to a domain. **Low-confidence guess; flagging for a human call rather than assuming.**

### 14. Getting Started on Mobile
**Content:** Notion's own default onboarding page. Not user content.
**Repo mapping guess:** N/A — exclude from migration.

---

## ⚠️ Flags for human review before anything moves into a git repo

This is the most important section — err toward flagging.

### Personal / family / medical / financial detail about named individuals
- **None found that rises to personal/medical/financial detail about third parties.** Paul Nielsen and Chris Nielsen appear only as HandyMansfield's business maintenance *clients* — their pages contain business property addresses and an LLC mailing address, which reads as ordinary B2B contact/CRM data for a maintenance business, not personal, medical, or financial detail about them as individuals. No medical information, no financial account details, no family information about any third party was found anywhere in the two levels of depth explored.
- **Nothing found under `Getting Started on Mobile`** beyond Notion's stock content (not opened in full detail, given it's clearly boilerplate, but the title and context give no reason to expect otherwise).

### Other sensitive content worth a human look before migration (not "personal/family/medical/financial about a named individual," but still shouldn't land in a public or semi-public GitHub issue tracker as-is)
- **Jeremy's own personal financial detail:** the Nasdara "Community, Website & Monetization" page states Ko‑fi payouts route to "Jeremy's personal accounts (PayPal/bank)" rather than a business entity. No account numbers are given, just the fact that personal (not business) payment accounts are in use. Flagging since it's financial detail about the workspace owner himself.
- **Live credentials/secrets embedded in plain text**, under Nasdara → Deployment & Hosting:
  - VPPAdminTools admin password (`NasdaraAdmin26`)
  - Vanilla BattlEye admin login/password pair
  - Jeremy's Steam64 ID
  - These are operational secrets, not personal data, but they clearly should **not** be copied into GitHub Issues verbatim — flagging because the instruction was to flag anything that shouldn't move into a git repo as-is, and credentials are the clearest case of that even outside the personal-data definition.

---

## Recommended next-step ordering

**Safe to migrate first — business/property data only, no personal-content flags, no embedded secrets:**
1. `handymansfield` domain (items 3–10 above: Paul Nielsen, Chris Nielsen, both compliance checklists, both client templates, the Hub, and the pricing research) — purely business/operational content, addresses are business contact data, nothing medical/financial about the clients.
2. Applewood Plaza — Portfolio (item 1) — commercial property operations data only (meters, leases, units), no personal-content flags. Just decide first whether it's `business-finance` or deserves its own repo, since the guess above is genuinely uncertain.

**Needs an explicit owner decision before anything is touched:**
1. **Nasdara — DayZ PVE Server** (item 2) — not because of personal-data risk, but because (a) the repo mapping is ambiguous between `site-chernarus` and `aegis-mods` and may need to be split by sub-topic rather than dumped into one repo, and (b) it contains live admin credentials and a Steam64 ID that must be stripped or redacted before any of this content is copied into GitHub Issues, and (c) it mentions Jeremy's personal (vs. business) payout accounts.
2. **Task List / Tasks / Projects** (items 11–13) — unclear whether these generic databases belong to any tracked business domain at all; a human should confirm whether they're migration-relevant before any issues get created from them.
