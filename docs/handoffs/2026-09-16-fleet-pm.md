# Handoff — fleet PM — 2026-09-16

**From:** the PM session of the 2026-09-15/16 overnight round (14 sessions, two teams, two leads).
**To:** the incoming PM, operating the structure in `standards/sessions/fleet_structure.md` — one PM
directly managing six workers, plus one combined peer-review/merge-authority seat.

**Owner direction that produced this handoff, verbatim:** *"I think we need to focus on the dashboard
and ops decision before everything else. Lets prepare a handoff to a pm that directly manages 6
workers, 1 peer review + merge authority."* and *"all needs to be documented in github."*

## 0. Your first priority

**The dashboard and the Ops Decision Queue come before everything else.** Not the merge queue, not
the runner migration, not the mod work. The owner said so explicitly and the reason is visible in
this document: almost every delay recorded below was a decision that existed but was not in front of
him, or an answer he had already given that the fleet did not see.

The artifact pack:

| Artifact | URL |
|---|---|
| Ops Decision Queue | https://claude.ai/artifact/1fMqNA1zdQKsq1FDEFvyzf |
| Fleet Status (incl. 93-task tracking + cost inventory) | https://claude.ai/artifact/AAiVG3MxK1r3yNgm8tzMmf |
| AEGIS Ops Roster (draft proposal) | https://claude.ai/artifact/U23uudrKWTT1RJ8rrews4N |
| Original 93-task authority artifact | https://claude.ai/artifact/WZsmRodvsHAbVFj54JghKf |

Known gap, agreed but not built: when the owner answers several cards in a burst, they drop straight
into collapsed history and the queue reads as empty. He hit exactly this and reported "I am on ops
decision queue and don't see any tasks" — the queue was genuinely empty because he had answered
everything. A "recently resolved" strip that ages out is the agreed fix.

**Do not repeat this PM's error here:** on seeing that, this PM inferred a filing/rendering bug and
dispatched a fix. A worker checked the actual `render()` code, found it filters correctly on
`status === 'resolved'`, and declined to build. There was no bug. Read the store *and* the render
path before concluding anything about that page.

## 1. Owner decisions already given (do not re-ask)

These were answered and, in several cases, re-asked by this PM anyway because they sat collapsed in
the queue. Read the store directly (`read_db`, collection `decisions`) before asking the owner
anything.

- **Shockbyte = $24.99/month**, can rise on upgrade. This is the bill for the actual product and was
  the last real gap in the cost inventory.
- **GitHub Actions stays deliberately UNFUNDED.** Failures are the intended migration signal. Never
  propose funding it, raising a limit, or enabling billing to turn a check green.
- **Get off GitHub-hosted runners — Option A:** all jobs onto the existing self-hosted VPS runners,
  stay on GitHub, keep workflow files and `gh` tooling. Not a platform migration.
- **VPS-CI runner install targets the new third VPS** `vps-e3b2612a.vps.ovh.ca`, *not* the hardened
  edge host — with an allowlist entry narrowly scoped to exactly the install commands, using the same
  admin-bot key pattern as the other two hosts.
- **OVH API token → GET-only** at the account level (owner action; converts a prose guarantee into an
  enforced control).
- **CLAUDE.md's PM-handoff policy is legitimate** — the owner directed it. Settled; do not reopen.
- **site-badlands content is real, not fabricated** — see §4.
- **Ops Roster:** redesign, and specifically: *"I want you to do a lessons learned from tonight to
  develop our roster and best practices documentation for every live session startup."* This document
  and `standards/sessions/fleet_structure.md` are the first half of that instruction.

## 2. Open owner decisions

1. **Actions token scope on 5 repos** — `aegis-mods`, `aegis-poi`, `aegis-pricing`, `site-badlands`,
   `claude-agents` still have `read` + `approve_pr=false`, which makes the review workflow fail at
   `startup_failure` (no logs). Repos with `write` run fine. This is a per-repo setting and an owner
   click; an agent attempt was classifier-blocked.
2. **be-rcon's orphaned job** — its only workflow inherits the self-hosted default, but be-rcon is
   deliberately barred from VPS runners. The job therefore queues forever and emits nothing. Either an
   explicit hosted override for that one repo, or accept that PR review does not run there.
3. **handymansfield scope** — a separate client business with its own back-office. It currently has an
   open PR in the merge queue and two workflow-migration items, all frozen pending a ruling on whether
   this fleet touches it at all.
4. **personal-finance / personal-growth workflow migration** — one-line diffs, but moving
   personal-financial repos onto the shared VPS runner is a trust-boundary decision, not an edit.
5. **Six Category-D worktrees** — proven redundant (see §3) but `git worktree remove` refuses on all
   six. Needs `--force`, and the standing never-force rule is the owner's.
6. **GitHub token scope for billing visibility** — reopened card. Low stakes now that Actions is
   deliberately unfunded; the one thing still genuinely unknown is whether a delinquent account
   disables Actions *wholesale* or only hosted runners. If wholesale, the unfunded posture needs
   revisiting.
7. **"Mass identity purging application"** — the owner asked a session to serve as "team lead R" over
   researchers whose output routes to an intake mechanism rather than back to the lead. The session
   declined to accept until it understood the work, correctly. A full-estate search (all repos, all
   memory files) found **zero references** to it anywhere. It is not documented; only the owner can
   explain it. Do not staff it, create an intake, or dispatch research into it before he does.
8. Older, still open: website #27 PayPal `hosted_button_id`; the financial C3-vs-D2 classification
   conflict; loot XML sync phase C/D.

**Not open — corrected after first draft:** review gates for MasterThread and ops-policies were
resolved at 07:15 as option (a), the standard gate in both repos. The drafts exist (MasterThread #61,
ops-policies #14). This handoff originally listed it as open because it was written before the answer
landed — which is itself the pattern §0 warns about: read the store before asserting anything is
undecided.

**How decisions are now filed (owner instruction):** *"I want the suggested option preselected for me
to approve, which is the ops making a choice and me reviewing and approving it."* Every card carries a
`recommendedOption` the page preselects, with who recommended it and why; approving is one click plus
a confirm, and overriding is one click away. **A card filed without a recommendation is an incomplete
card** — presenting options with no opinion pushes the analysis back onto the owner, which is the
thing this instruction corrects.

## 3. State of the work

**Runner migration (owner priority after the dashboard).** The estate is already ~85% on self-hosted
runners — `core` migrated itself on 2026-09-14. A full inventory of all 33 repos was completed: eight
repos have no workflows; the large majority are already self-hosted; every reusable-workflow caller
was checked individually and no hidden overrides to hosted runners exist. Real remaining items are
handymansfield ×2, personal-finance, personal-growth, `ops-policies/policy-ci.yml` (blocked on its own
Q15), and five template stubs — the stubs are being handled by **removing** their workflows rather
than repointing them, since four are empty scaffolds and the fifth has a plan but no code, and a
repointed workflow in a repo with no registered runner is a silent trap.

**Two halves of one missing thing** (both inventories in flight at handoff time, neither complete):
nothing states *what a self-hosted runner provides* (gh CLI, gitleaks, unzip and jq have each been
discovered missing one at a time, by someone noticing), and nothing states *which repos have a runner
registered* (at least six known to have none). Size both before patching either.

**Merge queue.** Sixteen entries, independently verified against live `gh`. Fifteen PRs merged
between 06:57 and 07:19Z via the desktop merge tool. Tier 4 (live production economy) is
`site-chernarus` #96/#94/#88 — the review/merge seat's tier call, not the PM's; this PM demoted them
to Tier 3 while re-keying the queue from memory and was corrected. `site-chernarus #93` is
CONFLICTING and needs a rebase against #94. `ops-infra #9` reports mergeable=CLEAN but is
content-blocked on a security finding — **do not read GitHub's green mergeable state as readiness.**

**aegis-poi #13** is parked and out of the queue. A single no-op `CallLater` registration kills the
module every time; a control run proved the module itself loads and works fine without one. The
compile fix in the PR is real and stays, but does not fix this. Two theories have been disproved by
evidence. Next step is one boot test comparing the same one-line `CallLater` under `aegis-mods`
(which boot-tests clean); if that does not resolve it, it goes to the owner as a question needing a
real DayZ engine answer rather than consuming more fleet time.

**Six Category-D worktrees** are proven redundant by direct content read: each holds ~695-699 files
of which only 3-4 differ from `origin/main`, and every differing file is either superseded prose whose
newer replacement is on main, or byte-identical to the head of open PR #55. The ~699 figure was never
699 unique files. This took three passes — the first checked only for *new* files and said nothing
about modified ones; the second named a handful without accounting for the rest.

**Policy stubs: nothing to fix.** The standing claim that near-empty `MasterThread/policies/` files
were misgrounding live agents is **withdrawn**. Only 5 of 29 stubs are cited at all; every apparent
hit was a false positive where the agent's auto-generated *description* line dropped a path prefix
while its actual grounding cited a real, populated file. The four dormant agents already carry a
stub-fallback mitigation merged 2026-09-15. Two planned PRs were cancelled as unnecessary.

## 4. Errors this PM made, recorded so they are not repeated

Each was caught by a worker reading the thing rather than reasoning about it.

1. **Halted site-badlands PR #7 as "fabricated content."** Searched `scr17`, `holosight`,
   `desertcamo`, found zero hits, and called it the most important finding of the night. Those are
   colloquial prose names; the real ClassNames `SCARH`, `HoloOptic` and `_Desert`-suffixed variants
   are present on `site-chernarus origin/main`. The first check was also against a stale local
   checkout — the exact trap being enforced against fleet-wide that night. PR #7 turned out to be the
   most carefully evidenced PR in the queue.
2. **Claimed no fleet session could build or sign a mod.** Verified three true facts and drew a false
   conclusion, escalating an unnecessary signing-key decision, while another session had already
   boot-tested using a throwaway `DSCreateKey` keypair.
3. **Proposed merging from stored approval records.** The merge seat declined; its reasoning was
   better and this PM withdrew entirely.
4. **Proposed routing the runner install through `Invoke-Ansible.ps1`** to dissolve a permission
   blocker. Investigation found it would have required weakening a *stronger* guard than the one being
   avoided — and separately, the owner had already answered that the install targets a different host
   entirely.
5. **Inferred a Decision Queue bug from a store dump** and dispatched a fix for a defect that did not
   exist.
6. **Duplicated work across teams twice**, both caught before waste.

The pattern: inference from partial reads, stated with confidence, in a role where statements get
acted on. The correction rate was high because workers pushed back. **Protect that** — the structure
document's rule 5 exists for this reason.

## 3a. Phase 0 and Phase 1 — verified state, 2026-09-16

The owner's stated primary goal for the session was **completing Phase 0 and Phase 1**. Verified
against live systems (OVH API, `gh api`, repo greps) rather than against the plan's own notes.

**Phase 0 — all ten tasks are owner-tier. No session can complete any of them.** The fleet's job is
reducing each to a single click and establishing which are already done.

- **DONE (verified live):** 0.1 two hosts; 0.3 budget envelope (cross-checked against real OVH cost);
  0.9 five private repos (checked against all five via `gh api`, not the note).
- **Decision made, infrastructure NOT confirmed running:** 0.2 Discord.
- **CANNOT VERIFY from any session — Anthropic Console has no API reachable from here:** 0.4 DayZ
  workspace; 0.6 the four new workspaces; 0.7 API-key creation disabled + spend limits. There is **no
  fleet-side obstacle** on any of these; each is the owner opening the Console.
- **0.8 hardware-key 2FA — Settings-UI only.** GitHub's API cannot expose *which* factor is enrolled,
  by design. 2FA has been on since 2026-09-14, but TOTP-only would not satisfy this task. The owner
  must look at `github.com/settings/security` himself. This is a real finding, not a gap in effort.
- **0.10 commercial terms / zero data retention:** inherently the owner's to read and decide.

**Phase 1 — a third category exists that the plan has no state for.**

- **DONE:** 1.1 (verified live).
- **CODE EXISTS, APPLIED STATE UNKNOWN — 1.2, 1.5, 1.6, 1.7, 1.8.** Real Ansible role code is in the
  repo, but whether it is applied to the live hosts cannot be confirmed without SSH access. This is
  neither done nor not-done and should not be recorded as either. Several of these carry plan notes
  saying "applied BY HAND" — that note is the only evidence, and it is exactly the self-referential
  trail this document warns about twice.
- **NOT DONE — 1.3 Cloudflare Tunnel + Access.** Zero references anywhere; never started. 1.4 depends
  on it.
- **NOT DONE but complete and QC-passed in unmerged PR #10 — 1.9 encrypted Postgres.** A ten-file
  implementation sitting unshipped. **This is the single clearest unlock in Phase 1.**
- **NOT DONE — 1.10 restic backups** (zero references); **1.11 first restore test** (the phase exit,
  blocked by 1.9 and 1.10).

**Therefore:** Phase 1 cannot close without (a) the SSH authorisation, which resolves five tasks from
unknown to known, (b) PR #10 merged and applied, (c) Cloudflare Tunnel built from nothing, and
(d) restic built from nothing. Phase 0 cannot close without the owner opening two consoles.

**Phase ordering note (owner, 2026-09-16):** *"i want discord before game zone now in the phases
order."* Structurally this already holds — the Discord bridge is 2.24 and Phase 2's own exit criterion
is *"a request sent over Discord becomes a signed, reviewed, deployed change"*, while the game zone is
Phase 3. The practical consequence is that the Discord path inside Phase 2 is **not optional bonus
work**: 2.24 depends on 1.5, which is one of the code-exists-apply-unknown tasks above.

## 4a. The single best explanation for the night's confusion

Verified by `git log`, not relayed. Commit `d8ce422` ("docs: PM-handoff usage policy, anti-polling
handshake, real-time telemetry") is real, committed, and authored by the owner's own account. It sits
on branch `agent/masterthread/fleet-pm-policies` — **never merged to `origin/main`, with no open PR** —
and that branch is what happens to be checked out in the single shared `GitHub\MasterThread` clone
that every session on this machine reads from.

So any session that read those files off disk was reading an unmerged branch's content as though it
were the adopted standard. No fabrication, no unauthorised edit, no malice anywhere in the chain — a
shared local checkout silently presented an unmerged branch as canonical to every session that read
it. That single fact accounts for most of the night's contradictory conclusions, including two
sessions reaching opposite verdicts about the same file.

This is why the verification rule "read `origin/<default>`, never a shared working tree" is first in
the structure document, and why it is worth more than it looks.

## 4b. State at PM close — read this before anything else in the doc

**The fleet is empty.** At close, `ListAgents` returned zero reachable sessions. Every worker, both
leads, QC and merge authority closed on the owner's full-rotation order. The PM narrated "waiting on
X" for several messages after X had already gone — the last drift of the round, and the reason
this section exists: **check `ListAgents` before asserting who is live.**

**There is no merge authority.** Nine PRs are open in MasterThread with nobody holding the merge
seat. That gap was not flagged by anyone before closing, including the PM. The incoming PM's first
staffing act is the combined review/merge seat.

### Handoff index — start here

| PR / file | Author seat | What it holds |
|---|---|---|
| **#60** (this doc + `standards/sessions/fleet_structure.md`) | PM | structure, decisions given/open, Phase 0/1 verified state, PM errors |
| **#61** | github-85 | `pr-review.yml` gate + lessons-learned narrative; the systemic branch-namespace finding |
| **#62** | github-54 | 40 previously-unversioned files archived (12 `_security-public` files deliberately excluded — see queue card) |
| **#63** | github-54 | `decision_queue_standard.md` — card schema, lifecycle, corrections trail |
| **#64** | github-1e | throwaway-signing-key boot-test commands; full aegis-poi #13 CallLater state; runner bug caught live on services #100 |
| **#65** | github-6b (QC) | merge-queue holds, four never-reviewed PRs, review method and limits |
| `GitHub\SESSION_HANDOFF_2026-09-16-merge-authority-queue.md` | github-8e | **local only** — incident timeline with captured-output evidence, dependsOn proof, sole-writer status of the dashboard `queue` panel |
| `GitHub\SESSION_HANDOFF_2026-09-16-team-a-lead.md`, `-github-f9.md`, `-github-4f.md`, `-github-4e-policy-stub-audit.md`, `-merge-authority-tool.md` | leads/workers | local only, same convention |

**Pre-existing open PRs, not part of this rotation:** #54 (the original provenance PR — superseded
by #60/#61, leave open as the historical record, do not merge), #55 (gated by dependsOn on #54,
proven working), #59 (REPOS.md do-first refresh).

### Unfinished, in order of consequence

1. **`merge-queue.json` + notes are committed locally but NOT on GitHub.** Commit `723e2f8`,
   worktree `_wt-MasterThread-merge-queue`, branch `agent/MasterThread/merge-queue-of-record`.
   github-8e's own classifier blocked the push as "Out-of-Place Publication" and asked a peer to
   push it. The PM refused — a session doing what another session's permissions denied is the
   laundering pattern the round enforced against. **Two open questions for the owner:** does a live
   merge queue belong in MasterThread at all (the classifier may be right; `services/ops/` is the
   nearer home), and the branch name sits in the colliding `agent/MasterThread/` namespace.
2. **The dashboard `queue` panel has no writer.** github-8e was its sole writer with no resync.
   It is now frozen on its last value and looks live. Successor either takes over the write shape
   (documented in github-8e's local handoff) or marks the panel dead on day one.
3. **The overnight headless sweep is designed, filed, and unapproved** — Decision Queue card
   `overnight-headless-sweep-approval`, PM recommendation preselected: a 2-hour, 2-agent proof run
   before the 6-hour sweep. Nothing runs until the owner approves and a fresh session implements the
   supervisor. The safeguards must be supervisor-enforced, never agent-judged.
4. **Phase 1 cannot advance without the SSH authorisation** (target: the new third VPS
   `vps-e3b2612a.vps.ovh.ca`, narrow allowlist — the owner answered this in the queue, but
   github-f9 closed without receiving the direct in-conversation confirmation it required, and its
   local handoff says so). A fresh session inherits both the answer and the bar.
5. **The shared `GitHub\MasterThread` checkout is parked on
   `agent/MasterThread/github-1e-handoff-2026-09-16`** — a departed session's branch. Read
   `origin/main`.
6. **Six scope-held PRs** (5 stub-repo workflow removals + handymansfield #10) — the owner resolved
   the scope card; apply its answer to those six rather than re-deriving.

### Owner decisions given during the close (not elsewhere in this doc)

- Full rotation: every session ends, memory documented in GitHub first.
- Workers scope to workstreams; leads hold current / on-deck / next per member; workers run
  subagents beneath them; no worker idles.
- Dashboard stays on current infrastructure (no merge of artifacts) *provided every panel updates
  on change*; wallboard mode and the second-monitor launcher exist; kiosk variant sketched, not built.
- Resolved cards age out (open → 20-min strip → archived at 4h), never deleted; `followUpPending`
  cards never archive.
- Phase 2 is bonus, not critical — but Discord precedes the game zone, so the Discord path inside
  Phase 2 (2.24, depends on 1.5) is not optional.

## 5. Sessions unaccounted for

`github-4e` is absent from `ListAgents` with no stand-down record and no closing report. Its last
substantive work was the 29-stub policies audit and the `_security-public` cross-reference that found
six duplicate filenames. Whether it exited cleanly, crashed, or was closed directly is unknown. Treat
its output as unverified until someone re-derives it.

## 6. Housekeeping

- The usage watcher lost visibility (HTTP 429, three consecutive polls). Unknown usage is treated as
  high; the fleet was held to read-only and small-diff work. Re-arm with the same `-Name` and confirm
  a real number before lifting.
- Do not invoke `AEGIS-Merge-Queue.ps1` — see the approval section of the structure document. Pending
  an owner ruling on who may start it.
- Cost inventory, for the record: Shockbyte $24.99/mo, OVH edge $10/mo, Anthropic API $0 lifetime for
  this program specifically, Claude PR review $0.46-$1.75 per run on Opus (Anthropic spend, **not**
  Actions spend — do not conflate them). Still unknown: two other OVH boxes, Cloudflare, both domains.
