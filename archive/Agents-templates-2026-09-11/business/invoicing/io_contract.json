{
  "agent_name": "Invoicing Agent",
  "domain": "business",
  "version": "0.1.0",
  "inputs": [
    {
      "name": "record",
      "type": "object",
      "description": "Primary input object for the Invoicing Agent.",
      "required": true
    },
    {
      "name": "context",
      "type": "object",
      "description": "Optional contextual data (identity, environment, linked resources).",
      "required": false
    }
  ],
  "outputs": [
    {
      "name": "decisions",
      "type": "object",
      "description": "Structured output describing what the Invoicing Agent decided or performed."
    },
    {
      "name": "logs",
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Human-readable log lines explaining key decisions."
    }
  ],
  "error_handling": {
    "on_invalid_input": "Return a validation error with an explanation; do not attempt to proceed.",
    "on_missing_context": "Proceed conservatively, explicitly stating assumptions and limitations."
  }
}